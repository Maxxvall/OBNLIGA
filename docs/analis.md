# Анализ оптимизаций и улучшений OBNLIGA

**Дата:** 2025-01-XX  
**Версия:** 1.0

---

## 1. Оптимизация Frontend

### 1.1 Проблема: Частые WebSocket ошибки в консоли

**Статус:** ⚠️ Ожидается

**Описание:**  
В консоли браузера появляются ошибки вида:
```
wsClient.ts:62 WebSocket connection to 'wss://...' failed
```

**Причина:**  
`WSClient` пытается подключиться к WebSocket серверу, который:
- Не запущен локально (dev режим на порту 5183)
- Или сервер на Render.com недоступен/перегружен

**Решение:**  
По Конституции проекта (раздел V), **WebSocket НЕ ИСПОЛЬЗУЕТСЯ** из-за ограничений Render.com. Вместо этого используется **HTTP Polling + ETag**.

**Рекомендации:**
1. Отключить автоматическое подключение WSClient в production
2. Добавить условие `VITE_WS_ENABLED=false` в `.env.production`
3. Или полностью удалить wsClient.ts если он не используется

### 1.2 Проблема: Повторные рендеры компонентов

**Статус:** 🔍 Требует анализа

**Описание:**  
Компоненты перерендериваются при каждом изменении стора Zustand.

**Рекомендации:**
```tsx
// ❌ Плохо - подписка на весь стор
const { matches, standings } = useLeagueStore()

// ✅ Хорошо - выборочная подписка
const matches = useLeagueStore(state => state.matches)
const standings = useLeagueStore(state => state.standings)
```

### 1.3 Мемоизация тяжёлых вычислений

**Рекомендации:**
- Использовать `useMemo` для фильтрации/сортировки списков
- Использовать `React.memo` для карточек в списках
- Избегать inline-функций в `onClick` внутри `.map()`

---

## 2. Переход LIVE → FINISHED: Race Condition анализ

### 2.1 Текущая архитектура

При завершении матча (`status: LIVE → FINISHED`) происходит:

1. **Admin/Judge** отправляет `PUT /api/admin/matches/:matchId` с `status: FINISHED`
2. **Backend** в `adminRoutes.ts:4870` вызывает:
   ```typescript
   if (body.status === MatchStatus.FINISHED && existing.status !== MatchStatus.FINISHED) {
     await handleMatchFinalization(matchId, request.server.log, { publishTopic })
   }
   ```
3. **handleMatchFinalization** (matchAggregation.ts:84+) выполняет в транзакции:
   - `rebuildClubSeasonStats` — пересчёт статистики клубов
   - `rebuildPlayerSeasonStats` — пересчёт статистики игроков
   - `updatePredictions` — расчёт прогнозов
   - `updateSeriesState` — обновление серии (playoff)
4. После транзакции:
   - `recalculateUserRatings` — обновление рейтинга пользователей
   - Инвалидация кэша

### 2.2 Потенциальные Race Conditions

| Сценарий | Риск | Вероятность |
|----------|------|-------------|
| Два админа одновременно завершают матч | Дублирование расчётов | Низкая |
| Событие добавляется во время финализации | Событие не учтено в статистике | Средняя |
| WebSocket публикация до завершения транзакции | Клиенты видят старые данные | Низкая |
| Пользователь делает прогноз во время финализации | Прогноз не учитывается | Низкая (прогнозы закрываются при LIVE) |

### 2.3 Защита от Race Conditions

**Уже реализовано:**
- ✅ Транзакция с `timeout: 20000` мс
- ✅ Проверка `existing.status !== MatchStatus.FINISHED` перед финализацией
- ✅ Прогнозы закрываются при переходе в LIVE

**Рекомендации для усиления:**
```typescript
// Добавить блокировку на уровне матча
await prisma.$executeRaw`SELECT * FROM "Match" WHERE id = ${matchId} FOR UPDATE`
```

---

## 3. Валидация событий матча (Реализовано ✅)

### 3.1 Проблема

Админ мог добавить гол игроку, который уже был удалён с поля (красная карточка или вторая жёлтая).

### 3.2 Решение

В `admin/src/components/tabs/MatchesTab.tsx` добавлена валидация:

```typescript
// Проверка: игрок не должен быть удалён с поля ранее
const ejectionTypes = ['RED_CARD', 'SECOND_YELLOW_CARD']
const scoringTypes = ['GOAL', 'PENALTY_GOAL', 'OWN_GOAL']

if (scoringTypes.includes(eventForm.eventType)) {
  const playerEjection = matchEvents.find(ev =>
    ev.playerId === eventForm.playerId &&
    ejectionTypes.includes(ev.eventType) &&
    ev.minute < eventForm.minute
  )
  if (playerEjection) {
    // Показать ошибку
  }
}
```

---

## 4. Оценка нагрузки API

### 4.1 Критические эндпоинты

| Эндпоинт | Частота | Кэш TTL | Оценка |
|----------|---------|---------|--------|
| GET /api/league/matches | Высокая | 1 час | ✅ Оптимизирован |
| GET /api/matches/:id/details | Средняя | 5 сек (LIVE) | ✅ ETag |
| GET /api/predictions/my | Средняя | 5 мин | ✅ Per-user |
| GET /api/leaderboard | Высокая | 5 мин | ✅ Shared cache |
| GET /api/user/me | Каждый запуск | 5 мин | ⚠️ Можно увеличить |

### 4.2 Redis использование

**Текущий лимит:** 50 соединений (Render.com Free)

**Рекомендации:**
- Использовать connection pooling
- Не создавать pub/sub подписки
- Хранить только hot data (ETag, session tokens)

---

## 5. UI/UX Улучшения (Реализовано ✅)

### 5.1 Прогнозы
- ✅ Компактные карточки прогнозов
- ✅ Короткие названия клубов из БД

### 5.2 Детали матча
- ✅ Новая цветовая схема (cyan #00f0ff вместо pink)
- ✅ Статистика с progress bars
- ✅ События с иконками (⚽ 🅿️ 🔴 🟨 ⛔)
- ✅ Свайп для составов команд
- ✅ Пульсирующий badge "LIVE"

### 5.3 Результаты команды
- ✅ Фильтр показывает только FINISHED матчи

---

## 6. Следующие шаги

### Приоритет: Высокий
1. [ ] Отключить WSClient в production или добавить флаг
2. [ ] Добавить `React.memo` на карточки матчей и прогнозов

### Приоритет: Средний
3. [ ] Аудит `useEffect` зависимостей
4. [ ] Оптимизация Zustand селекторов
5. [ ] Добавить `FOR UPDATE` блокировку в handleMatchFinalization

### Приоритет: Низкий
6. [ ] Мониторинг Redis connections
7. [ ] Добавить метрики latency для API

---

## 7. Метрики качества

| Метрика | Текущее | Цель |
|---------|---------|------|
| Console errors | 1-5 (WebSocket) | 0 |
| LCP (Largest Contentful Paint) | ~2s | <1.5s |
| Bundle size (gzip) | ~180KB | <150KB |
| API response p95 | ~300ms | <200ms |
| Redis memory | ~10MB | <25MB |
